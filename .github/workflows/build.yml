name: Build
on:
  pull_request:
    paths:
      - 'src/**'
      - '*.{json|js|ts}'
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install
      - run: |
          npm run build
